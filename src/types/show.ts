export interface IShowApi {
  show_id: string;
  title: string;
  image_url: string;
  image_original_url: string;
  explicit: string;
  author_id: string;
  last_episode_at: string;
  site_url: string;
}
